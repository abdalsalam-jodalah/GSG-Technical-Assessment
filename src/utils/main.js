import { calculateSumAndAverage } from "./arrayUtils-Task2.js";
import { removeDuplicates } from "./arrayUtils-Task3.js";

const numbers = [10, 20, 30, 40];
console.log(calculateSumAndAverage(numbers));

const strings = ["apple", "banana", "apple"];
console.log(removeDuplicates(strings));